{
  "name": "215webconnect",
  "version": "1.0.0",
  "description": "215 Web Connect website",
  "main": "functions/api.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && mkdir -p dist && cp -r public/* dist/ && cp theme-settings.json dist/"
  },
  "dependencies": {
    "@netlify/functions": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
