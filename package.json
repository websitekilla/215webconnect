{
  "name": "215webconnect",
  "version": "1.0.0",
  "description": "215 Web Connect website",
  "main": "functions/api.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && mkdir -p functions && cp express/server.js functions/api.js && cp -r public/* dist/ && cp theme-settings.json dist/"
  },
  "dependencies": {
    "@netlify/functions": "^1.6.0",
    "bcrypt": "^5.1.0",
    "connect-mongo": "^5.0.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "helmet": "^7.0.0",
    "mongodb": "^5.3.0",
    "mongoose": "^7.1.0",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
